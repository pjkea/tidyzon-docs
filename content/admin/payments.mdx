
<EndpointHero
  method="GET"
  path="/v1/admin/payments"
  title="List Payments"
  description="Returns a paginated list of payment transactions with aggregate statistics and applied filters."
/>

<TryItPanel
  method="GET"
  path="/v1/admin/payments"
  auth="admin"
  queryParams={[
    { name: 'limit', type: 'int', required: false, default: 10 },
    { name: 'offset', type: 'int', required: false, default: 0 },
    { name: 'status', type: 'string', required: false, description: 'Filter by payment status' },
    { name: 'start_date', type: 'string', required: false, description: 'YYYY-MM-DD' },
    { name: 'end_date', type: 'string', required: false, description: 'YYYY-MM-DD' },
    { name: 'min_amount', type: 'float', required: false },
    { name: 'max_amount', type: 'float', required: false },
  ]}
  sampleQuery={{ limit: 5 }}
/>

<ParamsCard
  title="Headers"
  params={[{ name: 'Authorization', type: 'string', required: true, description: 'Bearer {id_token}' }]}
/>

<ParamsCard
  title="Query Parameters"
  params={[
    { name: 'status', type: 'string', required: false },
    { name: 'start_date', type: 'string', required: false, description: 'YYYY-MM-DD' },
    { name: 'end_date', type: 'string', required: false, description: 'YYYY-MM-DD' },
    { name: 'min_amount', type: 'float', required: false },
    { name: 'max_amount', type: 'float', required: false },
    { name: 'limit', type: 'int', required: false, default: 10 },
    { name: 'offset', type: 'int', required: false, default: 0 },
  ]}
/>

<ResponseExplorer
  responses={[
    {
      status: 200,
      fields: [
        { name: 'data', type: 'object', fields: [
          { name: 'payments[]', type: 'array', description: 'Empty array when no payments match the filter' },
          { name: 'pagination', type: 'object', fields: [
            { name: 'total_count', type: 'int' },
            { name: 'returned_count', type: 'int' },
            { name: 'limit', type: 'int' },
            { name: 'offset', type: 'int' },
          ]},
          { name: 'statistics', type: 'object', fields: [
            { name: 'total_transactions', type: 'int' },
            { name: 'total_amount', type: 'float' },
            { name: 'average_amount', type: 'float' },
          ]},
          { name: 'filters_applied', type: 'object', fields: [
            { name: 'status', type: 'string | null' },
            { name: 'start_date', type: 'string | null' },
            { name: 'end_date', type: 'string | null' },
            { name: 'min_amount', type: 'float | null' },
            { name: 'max_amount', type: 'float | null' },
          ]},
        ]},
      ],
      sample: {
        message: "Payments retrieved successfully",
        data: {
          payments: [],
          pagination: { total_count: 0, returned_count: 0, limit: 5, offset: 0 },
          statistics: { total_transactions: 0, total_amount: 0.0, average_amount: 0.0 },
          filters_applied: { status: null, start_date: null, end_date: null, min_amount: null, max_amount: null }
        }
      }
    },
    { status: 401, description: 'Unauthorized.', sample: { message: "Unauthorized" } }
  ]}
/>

---

<EndpointHero
  method="GET"
  path="/v1/admin/payments/{paymentid}"
  title="Get Payment"
  description="Returns the full detail for a single payment transaction."
/>

<TryItPanel
  method="GET"
  path="/v1/admin/payments/{paymentid}"
  auth="admin"
  pathParams={[{ name: 'paymentid', type: 'int', required: true, description: 'Payment ID', example: '1' }]}
/>

<ResponseExplorer
  responses={[
    {
      status: 200,
      fields: [
        { name: 'data', type: 'object', description: 'Full payment record' },
      ],
      sample: { message: "Payment retrieved successfully", data: {} }
    },
    { status: 404, description: 'Payment not found.', sample: { message: "Payment not found" } }
  ]}
/>
