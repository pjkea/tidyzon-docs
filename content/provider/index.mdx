# Provider API

The Provider endpoints serve the Tidyzon provider (Tidysp) app — managing vehicles, completing service requests, uploading wash photos, and tracking earnings.

**Auth:** User JWT required. Obtain via `POST /v1/auth/sign-in` with provider credentials.
